
export enum Roles {
  // The basic role which all users have when logging in.
  USER = "user",

  // A role that lets users perform administrative actions such as managing other users.
  ADMIN = "admin"
}
